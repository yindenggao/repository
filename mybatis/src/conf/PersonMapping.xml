<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<!-- 
		#{id}:从传递过来的参数中取出id
	 -->
	<!-- <mapper namespace="cn.itcast.mybatis.PersonMapping">
		<select id="selectPerson" resultType="cn.itcast.domain.Person">
			select id,name goodName from PERSON where id = #{id}
		</select>
	</mapper> -->
	
	<mapper namespace="cn.itcast.mybatis.test.Mybatis">
		<select id="getPersonId" resultType="cn.itcast.domain.Person">
			select id,name goodName from PERSON where id = #{id}
		</select>
		
	</mapper>
	